package com.example.interapi.usecase.driver;

public class DeleteDriverUseCase {

    /*
     * Constraints:
     *
     * - Driver must exist
     * - Driver must not be assigned to any route
     * - None of the Driver's vehicles must be assigned to any route
     * - None of the Driver's vehicles must be in status = IN_USE
     */
}
